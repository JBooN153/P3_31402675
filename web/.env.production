# Variables de entorno para producción
# Cuando está desplegado, el frontend se sirve desde el mismo servidor
# Por lo que no necesita una URL base específica (usa window.location.origin)
# Si necesitas cambiar a otro servidor, configura esto:
# VITE_API_URL=https://tu-api-produccion.com
