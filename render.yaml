services:
  - type: web
    name: P3-31899312-App
    env: node
    plan: free
    buildCommand: npm ci && npm run build && echo "✓ Build completado - public/ contents:" && ls -la public | head -5
        value: ./config/database.sqlite
    routes:
      - path: /
        matchType: prefix
