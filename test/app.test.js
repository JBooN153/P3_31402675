// This file exists for compatibility with older test globs.
// Keep a single trivial test so Jest doesn't fail when matching this path.
test('placeholder - legacy test file', () => {
	expect(true).toBe(true);
});
